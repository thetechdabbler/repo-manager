{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Home","text":"<p>Multi-repo git, safely</p> <ul> <li> <p> Install in seconds</p> <p>One <code>pipx</code> command from Git. Python 3.11+ and nothing else to configure.</p> <p> Installation</p> </li> <li> <p> See everything at once</p> <p>Branch, state, and local work across every repository in a single table.</p> <p> Daily status</p> </li> <li> <p> Safe by default</p> <p>Only clean, tracking, fast-forwardable repos change. Every skip states a reason and a next action.</p> <p> Safety model</p> </li> <li> <p> Every command, documented</p> <p>A full reference generated from the CLI itself, so it can never drift from the tool.</p> <p> Command reference</p> </li> </ul>"},{"location":"#repo-manager","title":"repo-manager","text":"<p>One workspace, many repositories, no surprises. Inspect, update, and summarize a tree of independent Git repos. Only clean, fast-forwardable repos ever change, and every skip tells you why.</p> <p>Get started Command reference</p>"},{"location":"#the-mental-model","title":"The mental model","text":"<p>Two ideas explain almost everything about how the tool behaves.</p> <p>Discover broadly, operate narrowly. <code>init</code> scans a workspace and finds every repository. Mutations only ever touch repositories you have selected, and only when they are safe to touch.</p> <p>Skip with a reason. A clean, tracking worktree that can fast-forward is the only thing updated by default. Anything else (dirty, detached, diverged, mid-rebase) is skipped, and every skip states why and what to do next. A skipped repository is a result, not a failure to hide.</p> <pre><code>SKIPPED  services/payments-api\nReason:  4 local change(s) would be at risk\nNext:    commit or stash the changes, or use --stash-and-update\n</code></pre>"},{"location":"#60-second-tour","title":"60-second tour","text":"<pre><code># Scan a workspace and save a profile (no git mutation).\nrepo-manager init ~/work/services --name services --yes\n\n# See the state of every repository.\nrepo-manager status --fetch\n\n# Preview a safe update: what would change, and why the rest is skipped.\nrepo-manager update --dry-run\n\n# Fast-forward the eligible repositories.\nrepo-manager update --yes\n</code></pre> <p>See Installation to get started, or the Safety model for exactly what is and is not touched.</p>"},{"location":"contributing/","title":"Contributing","text":""},{"location":"contributing/#development-setup","title":"Development setup","text":"<pre><code>python -m venv .venv\n.venv/bin/pip install -e '.[dev]'\n.venv/bin/pytest\n</code></pre>"},{"location":"contributing/#the-fixture-matrix","title":"The fixture matrix","text":"<p>The test suite is built fixture-first. <code>tests/conftest.py</code> constructs a workspace of independent repositories, each in one specific state (clean-behind, dirty, detached, diverged, mid-rebase, unreachable remote, and so on) from local bare remotes, with a neutralized git environment so a contributor's own git config cannot change the outcome.</p> <p>The <code>MATRIX</code> in that file is the single source of truth for expected state. The state model, the policy engine, and the CLI are all asserted against it. When you add a state or change a classification, update the matrix first; it is the specification.</p>"},{"location":"contributing/#generated-docs","title":"Generated docs","text":"<p>Two pages are generated from the code and must not be edited by hand:</p> <ul> <li><code>docs/reference/commands.md</code> from the CLI definitions</li> <li><code>docs/concepts/safety-model.md</code> from the state enum and the policy functions</li> </ul> <p>Regenerate and validate them with:</p> <pre><code>make docs          # regenerate\nmake docs-check    # fail if committed copies are stale (also run in CI)\n</code></pre> <p>The policy table in the safety model is produced by running the real decision functions over a representative snapshot per state, so the documentation cannot drift from the tested behavior.</p>"},{"location":"contributing/#before-opening-a-pull-request","title":"Before opening a pull request","text":"<pre><code>make test\nmake docs-check\n</code></pre>"},{"location":"contributing/#design","title":"Design","text":"<p>The architecture and phase plan live in <code>repo-manager-architecture-final.md</code> at the repository root. It records the rationale behind the safety model, the concurrency choices, and what is deliberately deferred.</p>"},{"location":"installation/","title":"Installation","text":""},{"location":"installation/#requirements","title":"Requirements","text":"<ul> <li>Python 3.11 or newer</li> <li>Git on your <code>PATH</code></li> </ul>"},{"location":"installation/#install","title":"Install","text":"<p>The tool is a standalone CLI, best installed globally with pipx so it stays isolated from your project environments.</p> <p>It is installed directly from the Git repository:</p> <pre><code>pipx install \"git+https://github.com/thetechdabbler/repo-manager.git\"\n</code></pre> <p>To upgrade later (pipx re-pulls from the same source):</p> <pre><code>pipx upgrade repo-manager\n</code></pre> <p>Not on PyPI</p> <p><code>repo-manager</code> is not published to the Python Package Index. Running <code>pipx install repo-manager</code> would fetch an unrelated, abandoned project of the same name and fail to build. Always install from the Git URL above.</p> <p>To install a specific branch, or a local clone you are developing against:</p> <pre><code>pipx install \"git+https://github.com/thetechdabbler/repo-manager.git@&lt;branch&gt;\"\npipx install /path/to/repo-manager\n</code></pre>"},{"location":"installation/#verify","title":"Verify","text":"<pre><code>repo-manager version\nrepo-manager help\n</code></pre> <p><code>repo-manager help</code> prints a one-screen manual: every command, the repository states, and the exit codes.</p>"},{"location":"installation/#configuration-location","title":"Configuration location","text":"<p>Profiles are stored under your XDG config directory, one file per workspace:</p> <pre><code>~/.config/repo-manager/\n\u251c\u2500\u2500 config.toml            # global settings and the active project\n\u2514\u2500\u2500 projects/\n    \u2514\u2500\u2500 services.toml      # one profile per workspace\n</code></pre> <p>Set <code>REPO_MANAGER_HOME</code> to override that location (useful for testing or for keeping profiles in a dotfiles repository).</p> <p>See Configuration for the profile schema.</p>"},{"location":"concepts/configuration/","title":"Configuration","text":"<p>A profile describes one workspace: where it is, which repositories belong to it, how they group, and the policy for operating on them. Profiles are TOML, written with comment-preserving formatting so you can annotate them by hand.</p>"},{"location":"concepts/configuration/#creating-a-profile","title":"Creating a profile","text":"<p><code>init</code> discovers repositories and writes the profile for you. It performs no git mutation.</p> <pre><code>repo-manager init ~/work/services --name services\n</code></pre> <p>Interactively it shows a selectable table with each repository's branch and inferred default. Non-interactively, <code>--yes</code> selects everything discovered.</p>"},{"location":"concepts/configuration/#profile-schema","title":"Profile schema","text":"<pre><code>schema_version = 1\n\n[project]\nname = \"services\"\nroot = \"/Users/alex/work/services\"\ndefault_remote = \"origin\"\n\n[discovery]\nmax_depth = 4\ninclude_root_repository = true\nexclude = [\".git\", \".venv\", \"node_modules\", \"outputs\", \".pytest_cache\"]\ninclude_linked_worktrees = false\n\n[policy]\npull_mode = \"ff-only\"\nskip_dirty = true\nfetch_before_update = true\nfetch_before_status = false\nfetch_timeout_seconds = 30\njobs = 8\n\n[[repositories]]\npath = \".\"\nname = \"workspace-root\"\ndefault_branch = \"main\"\ngroups = [\"root\"]\n\n[[repositories]]\npath = \"services/payments-api\"\ndefault_branch = \"dev\"\ngroups = [\"services\", \"payments\"]\nremote = \"origin\"\n</code></pre>"},{"location":"concepts/configuration/#repositories","title":"Repositories","text":"<p>Each <code>[[repositories]]</code> entry has a <code>path</code> relative to <code>root</code> (paths outside the root are rejected), an optional <code>name</code>, an optional <code>groups</code> list, an optional <code>default_branch</code> override, and an optional <code>remote</code> override.</p> <p>Groups are defined exactly one way: the per-repository <code>groups</code> array. Any command that accepts <code>--group</code> matches against it.</p>"},{"location":"concepts/configuration/#default-branch-inference","title":"Default-branch inference","text":"<p>When you do not set <code>default_branch</code>, it is inferred in this order, and the source is recorded so you can tell a proven default from a heuristic:</p> <ol> <li>A profile override.</li> <li><code>refs/remotes/&lt;remote&gt;/HEAD</code>.</li> <li>The current branch's upstream, if it is a common default name.</li> <li>An existing <code>main</code>, then <code>master</code>, then <code>dev</code>.</li> <li>Otherwise <code>ambiguous</code>, which shows as <code>default-branch-unknown</code> and requires you to    set the value.</li> </ol>"},{"location":"concepts/configuration/#storage-and-overrides","title":"Storage and overrides","text":"<p>Profiles live under <code>~/.config/repo-manager/projects/</code>. Set <code>REPO_MANAGER_HOME</code> to relocate that directory.</p>"},{"location":"concepts/safety-model/","title":"Safety model","text":""},{"location":"concepts/safety-model/#safety-model","title":"Safety model","text":"<p>Every repository is reduced to exactly one state. Each operation then decides, per state, whether it may proceed or must skip with a reason.</p>"},{"location":"concepts/safety-model/#repository-states","title":"Repository states","text":"State Meaning <code>ready</code> Clean, tracking, behind. The only state eligible for a default fast-forward update. <code>current</code> Clean and up to date with its upstream. <code>dirty</code> Uncommitted staged, unstaged, or untracked changes are present. <code>in-progress</code> A rebase, merge, cherry-pick, revert, or bisect is in progress. Never mutated. <code>detached</code> HEAD points at a commit rather than a branch. <code>no-upstream</code> The current branch has no tracking configuration. <code>default-branch-unknown</code> The default branch could not be inferred. Blocks switch-default. <code>ambiguous-remote</code> Several remotes exist and none is named 'origin'. Needs configuration. <code>ahead</code> Local commits are not on the remote; a fast-forward is not possible. <code>diverged</code> Local and upstream have both moved. Needs manual reconciliation. <code>remote-unavailable</code> A fetch failed, so remote state is unknown. Never guessed from stale refs."},{"location":"concepts/safety-model/#policy-by-operation","title":"Policy by operation","text":"<p>Generated by running the real decision functions over one representative snapshot per state.</p> State <code>update</code> <code>switch-default</code> <code>checkout</code> <code>ready</code> proceed proceed proceed <code>current</code> proceed proceed proceed <code>dirty</code> skip skip proceed <code>in-progress</code> skip skip skip <code>detached</code> skip skip proceed <code>no-upstream</code> skip proceed proceed <code>default-branch-unknown</code> proceed skip proceed <code>ambiguous-remote</code> skip skip proceed <code>ahead</code> skip proceed proceed <code>diverged</code> skip proceed proceed <code>remote-unavailable</code> skip skip proceed <p><code>checkout</code> is permissive by design: it only pre-skips an in-progress repository and a branch that exists nowhere. A dirty tree that would be overwritten is refused by git itself and reported as a failure.</p>"},{"location":"guides/daily-status/","title":"Daily status","text":"<p><code>status</code> is the command you run most. It is read-only.</p> <pre><code>repo-manager status\n</code></pre> <p>Without a network call it reports each repository's branch, cleanliness, and last commit. The ahead/behind column shows <code>no-fetch</code>, because the tool refuses to present stale remote-tracking references as if they were current.</p>"},{"location":"guides/daily-status/#getting-real-aheadbehind-numbers","title":"Getting real ahead/behind numbers","text":"<p>Pass <code>--fetch</code> to update remote-tracking refs first:</p> <pre><code>repo-manager status --fetch\n</code></pre> <p>Fetches run in parallel (see <code>--jobs</code>) and each has a timeout, so an unreachable or credential-gated remote reports <code>remote-unavailable</code> rather than hanging the run.</p>"},{"location":"guides/daily-status/#narrowing-the-view","title":"Narrowing the view","text":"<pre><code>repo-manager status --group backend        # one group\nrepo-manager status --repo api             # one repository\nrepo-manager status --select dirty         # only repos with local work\n</code></pre> <p><code>--select</code> accepts a comma-separated union of tokens: <code>all</code>, <code>clean</code>, any state name (<code>dirty</code>, <code>ready</code>, <code>ahead</code>, ...), <code>group:NAME</code>, <code>search:TEXT</code>, and <code>name:NAME</code>.</p>"},{"location":"guides/daily-status/#machine-readable-output","title":"Machine-readable output","text":"<pre><code>repo-manager status --json\n</code></pre> <p>The JSON is a stable, versioned structure (<code>schema_version</code>). It is the contract for scripts and for any tooling built on top of the CLI; the terminal table is just one rendering of the same report.</p>"},{"location":"guides/daily-status/#reading-the-columns","title":"Reading the columns","text":"Column Meaning Branch Current branch, or <code>detached@&lt;sha&gt;</code> State One of the repository states \u2191/\u2193 Commits ahead / behind upstream, or <code>no-fetch</code> / <code>unreachable</code> Changes <code>Ns</code> staged, <code>Nm</code> modified, <code>N?</code> untracked, <code>Nu</code> unmerged Last commit Short SHA and subject"},{"location":"guides/recovery/","title":"Recovering local work","text":"<p>By default, a dirty repository is skipped by <code>update</code> and <code>switch-default</code> so your local work is never touched. When you do want to update on top of local changes, <code>--stash-and-update</code> is the opt-in, conflict-safe path.</p> <pre><code>repo-manager update --stash-and-update --group backend\nrepo-manager switch-default --stash-and-update --repo api\n</code></pre> <p>For each dirty repository that is otherwise fast-forwardable, it:</p> <ol> <li>Creates a uniquely named stash, including untracked files.</li> <li>Fast-forwards using the normal safe path (<code>switch-default</code> also switches branch first).</li> <li>Restores the stash.</li> <li>Reports whether the restore was clean or conflicted.</li> </ol>"},{"location":"guides/recovery/#when-the-restore-is-clean","title":"When the restore is clean","text":"<p>The stash is applied and dropped, the repository shows as <code>updated</code>, and your local changes sit on top of the freshly fast-forwarded branch. Nothing more to do.</p>"},{"location":"guides/recovery/#when-the-restore-conflicts","title":"When the restore conflicts","text":"<p>The stash is never dropped on a conflict. The repository is reported as <code>failed</code>, and the result carries the exact recovery commands:</p> <pre><code>FAILED  services/payments-api\nError:  the operation succeeded but restoring your local changes conflicted\nNext:   your changes are safe in a stash. Inspect with\n        `git -C &lt;path&gt; stash list` (entry: 'repo-manager auto-stash: ...');\n        re-apply after resolving with `git -C &lt;path&gt; stash pop stash@{0}`\n</code></pre> <p>The fast-forward still happened; your uncommitted work is preserved in the named stash and can be recovered at any time. In <code>--json</code> output the same information is available under each result's <code>stash</code> object (<code>stashed</code>, <code>reference</code>, <code>restore</code>).</p>"},{"location":"guides/recovery/#what-is-never-stashed","title":"What is never stashed","text":"<p>Stashing only applies to a <code>dirty</code> repository whose branch can fast-forward. Anything that stashing cannot make updatable is still skipped with a reason:</p> <ul> <li>A repository mid rebase, merge, cherry-pick, revert, or bisect (<code>in-progress</code>) is   never touched, even with this flag.</li> <li>A <code>diverged</code> or <code>ahead</code> branch, where a fast-forward is impossible regardless of the   working tree.</li> </ul>"},{"location":"guides/summaries/","title":"Summaries","text":"<p><code>summary</code> answers \"what changed across the workspace recently?\" It is read-only and produces the same structured report in terminal, JSON, and Markdown form.</p> <pre><code>repo-manager summary --since 7d\nrepo-manager summary --since 2026-07-01 --group backend\nrepo-manager summary --since 7d --markdown report.md\n</code></pre>"},{"location":"guides/summaries/#the-window","title":"The window","text":"<p><code>--since</code> accepts duration shorthand (<code>7d</code>, <code>2w</code>, <code>24h</code>), an ISO date (<code>2026-07-01</code>), or any expression git understands (<code>yesterday</code>, <code>2 weeks ago</code>). It defaults to <code>7d</code>.</p> <p>Date rule: commits are selected by committer date, first-parent, on each repository's currently checked-out branch (HEAD). There is no stored review cursor, so nothing goes stale after a rebase; the range is always derived from <code>--since</code>.</p>"},{"location":"guides/summaries/#what-it-collects","title":"What it collects","text":"<p>Per repository, and aggregated across the workspace:</p> <ul> <li>Commit SHA, author, committer date, and subject.</li> <li>Insertions, deletions, and files changed per commit and in total.</li> <li>The top-level directories touched, ordered by how many files changed in each.</li> <li>Detected references: JIRA-style ticket keys (<code>ABC-123</code>) and conventional-commit   types (<code>feat</code>, <code>fix</code>, ...) parsed from subjects.</li> </ul>"},{"location":"guides/summaries/#output","title":"Output","text":"<ul> <li>Default: a terminal table with a per-repository commit breakdown.</li> <li><code>--json</code>: the full structured <code>SummaryReport</code> (stable, versioned) for automation.</li> <li><code>--markdown &lt;path&gt;</code>: a handoff-ready Markdown report, ideal for a standup note or a   review summary.</li> </ul> <p>The three renderers are views of one structure, so the JSON is the contract and the table and Markdown never diverge from it.</p>"},{"location":"guides/summaries/#selecting-repositories","title":"Selecting repositories","text":"<p><code>--group</code>, <code>--repo</code>, and <code>--select</code> apply. For summaries, <code>--select</code> supports <code>all</code>, <code>changed</code>, <code>quiet</code>, <code>name:NAME</code>, <code>search:TEXT</code>, and a bare name or path; use <code>--group</code> to scope by group.</p>"},{"location":"guides/updating/","title":"Updating","text":"<p>Two commands change branch state. Both fetch in parallel first, then mutate serially and locally, so they never block on the network mid-operation. Both are safe by default: only eligible repositories are touched, and everything else is skipped with a reason.</p>"},{"location":"guides/updating/#update-fast-forward-the-current-branch","title":"<code>update</code> \u2014 fast-forward the current branch","text":"<p><code>update</code> fast-forwards the branch each repository is already on. It never switches branches.</p> <pre><code>repo-manager update --dry-run     # preview the plan, change nothing\nrepo-manager update --yes         # execute\nrepo-manager update --group libraries --yes\n</code></pre> <p>Only <code>ready</code> repositories (clean, tracking, behind) are updated. See the policy table for what happens in every other state.</p>"},{"location":"guides/updating/#switch-default-return-to-the-default-branch","title":"<code>switch-default</code> \u2014 return to the default branch","text":"<p><code>switch-default</code> switches each repository to its configured default branch and fast-forwards it. Useful for returning a whole workspace to a clean baseline after feature work.</p> <pre><code>repo-manager switch-default --dry-run\nrepo-manager switch-default --select clean --yes\n</code></pre> <p>If the default branch itself has diverged from its upstream, the repository is reported as failed rather than silently merged.</p>"},{"location":"guides/updating/#previewing-and-confirming","title":"Previewing and confirming","text":"<ul> <li><code>--dry-run</code> runs no mutation command. It prints exactly what would happen.</li> <li>Interactively, a plan table is shown and a single per-batch confirmation is asked.</li> <li>Non-interactively, you must pass <code>--yes</code> (or <code>--dry-run</code>). The tool refuses to   mutate silently in a script without explicit consent.</li> </ul>"},{"location":"guides/updating/#selecting-repositories","title":"Selecting repositories","text":"<p><code>--group</code>, <code>--repo</code>, and <code>--select</code> all apply, exactly as in status.</p>"},{"location":"guides/updating/#exit-codes","title":"Exit codes","text":"Code Meaning 0 Everything completed or was already current 1 One or more repositories failed 3 One or more repositories were safety-skipped <p>Exit 3 is intentional: a routine dirty-repo skip makes the whole run non-zero so it cannot be ignored in automation. Pass <code>--ignore-skips</code> to downgrade a skip-only run to 0 for shell prompts and non-blocking scripts.</p>"},{"location":"reference/commands/","title":"Commands","text":""},{"location":"reference/commands/#command-reference","title":"Command reference","text":"<p>Generated from repo-manager <code>0.1.0</code>.</p>"},{"location":"reference/commands/#checkout","title":"<code>checkout</code>","text":"<p>Check out a branch across selected repositories, creating tracking branches as needed.</p> <pre><code>repo-manager checkout [OPTIONS]\n</code></pre> Option Description <code>--branch</code> Branch to check out, or 'default' for each repo's default. <code>--project</code> Profile name; defaults to active. <code>--group</code> Limit to a repository group. <code>--repo</code> Limit to one repository. <code>--select</code> Selection expression. <code>--dry-run</code> Preview; make no changes. <code>--yes</code>, <code>-y</code> Skip the confirmation prompt. <code>--ignore-skips</code> Exit 0 even when repos are safety-skipped. <code>--jobs</code> Parallel workers for fetches. <code>--json</code> Emit JSON instead of a table. <code>--help</code> Show this message and exit."},{"location":"reference/commands/#help","title":"<code>help</code>","text":"<p>Show a quick manual with commands, states, and examples.</p> <pre><code>repo-manager help [OPTIONS]\n</code></pre> Option Description <code>--help</code> Show this message and exit."},{"location":"reference/commands/#init","title":"<code>init</code>","text":"<p>Discover a workspace and write a profile. Performs no git mutation.</p> <pre><code>repo-manager init [OPTIONS] {path}\n</code></pre> Option Description <code>--name</code> Profile name; defaults to the dir name. <code>--max-depth</code> Maximum discovery depth. (default: <code>4</code>) <code>--remote</code> Default remote name. (default: <code>origin</code>) <code>--include-linked-worktrees</code> Include linked worktrees. <code>--yes</code>, <code>-y</code> Skip interactive selection; take all. <code>--activate</code> Set this profile as the active project. (default: <code>True</code>) <code>--help</code> Show this message and exit."},{"location":"reference/commands/#profiles","title":"<code>profiles</code>","text":"<p>List known profiles and mark the active one.</p> <pre><code>repo-manager profiles [OPTIONS]\n</code></pre> Option Description <code>--help</code> Show this message and exit."},{"location":"reference/commands/#status","title":"<code>status</code>","text":"<p>Show repository status. Read-only unless --fetch is passed.</p> <pre><code>repo-manager status [OPTIONS]\n</code></pre> Option Description <code>--project</code> Profile name; defaults to active. <code>--group</code> Limit to a repository group. <code>--repo</code> Limit to one repository. <code>--fetch</code> Fetch before computing remote state. <code>--jobs</code> Parallel workers for reads/fetches. <code>--json</code> Emit JSON instead of a table. <code>--help</code> Show this message and exit."},{"location":"reference/commands/#summary","title":"<code>summary</code>","text":"<p>Summarize commits across repositories since a date. Read-only.</p> <pre><code>repo-manager summary [OPTIONS]\n</code></pre> Option Description <code>--since</code> Window: 7d, 2w, 24h, an ISO date, or 'yesterday'. (default: <code>7d</code>) <code>--project</code> Profile name; defaults to active. <code>--group</code> Limit to a repository group. <code>--repo</code> Limit to one repository. <code>--select</code> Selection expression. <code>--markdown</code> Write a Markdown report to this path. <code>--jobs</code> Parallel workers. <code>--json</code> Emit JSON instead of a table. <code>--help</code> Show this message and exit."},{"location":"reference/commands/#switch-default","title":"<code>switch-default</code>","text":"<p>Switch each eligible repository to its default branch and fast-forward it.</p> <pre><code>repo-manager switch-default [OPTIONS]\n</code></pre> Option Description <code>--project</code> Profile name; defaults to active. <code>--group</code> Limit to a repository group. <code>--repo</code> Limit to one repository. <code>--select</code> Selection expression. <code>--dry-run</code> Preview; make no changes. <code>--yes</code>, <code>-y</code> Skip the confirmation prompt. <code>--ignore-skips</code> Exit 0 even when repos are safety-skipped. <code>--stash-and-update</code> For dirty repos: stash, switch and fast-forward, then restore. <code>--jobs</code> Parallel workers for fetches. <code>--json</code> Emit JSON instead of a table. <code>--help</code> Show this message and exit."},{"location":"reference/commands/#update","title":"<code>update</code>","text":"<p>Fast-forward the current branch of eligible repositories. Never switches branches.</p> <pre><code>repo-manager update [OPTIONS]\n</code></pre> Option Description <code>--project</code> Profile name; defaults to active. <code>--group</code> Limit to a repository group. <code>--repo</code> Limit to one repository. <code>--select</code> Selection expression, e.g. 'ready', 'dirty', 'group:libraries'. <code>--dry-run</code> Preview; make no changes. <code>--yes</code>, <code>-y</code> Skip the confirmation prompt. <code>--ignore-skips</code> Exit 0 even when repos are safety-skipped. <code>--stash-and-update</code> For dirty repos: stash, fast-forward, then restore (conflict-safe). <code>--jobs</code> Parallel workers for fetches. <code>--json</code> Emit JSON instead of a table. <code>--help</code> Show this message and exit."},{"location":"reference/commands/#version","title":"<code>version</code>","text":"<p>Print the version.</p> <pre><code>repo-manager version [OPTIONS]\n</code></pre> Option Description <code>--help</code> Show this message and exit."},{"location":"reference/commands/#exit-codes","title":"Exit codes","text":"Code Meaning 0 All requested operations completed or were already current. 1 One or more repositories failed. 2 Invalid command, configuration, or selection. 3 One or more operations were safety-skipped (see <code>--ignore-skips</code>)."}]}